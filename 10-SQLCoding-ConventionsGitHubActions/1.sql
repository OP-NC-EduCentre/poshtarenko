CREATE TABLE Furniture (
    Id NUMBER(9),
    Name VARCHAR(30),
    Type_Code NUMBER(3),
    Producer VARCHAR(30),
    Amount NUMBER(5),
    Production_Time NUMBER(3), -- in days
    Price NUMBER(9, 2)
);
